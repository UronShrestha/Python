rows = int(input("Enter the number of rows : "))
columns = int(input("Enter the number of columns : "))
symbol = input("Input the desired symbol : ")

for row in range(rows):
    for column in range(columns):
        print(symbol, end="")
    print()

     
     

